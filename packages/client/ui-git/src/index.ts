/**
 * Git workbench plugin, node half.
 *
 * The right-Sidebar Git tab is browser presentation over future host services
 * (workspace changes, diff, commit); the node half has nothing to mount yet, so
 * it stays the empty apply the loader seat expects.
 */

/** Host plugin body — no host surface yet; the browser half owns the tab. */
export function apply(): void {}
