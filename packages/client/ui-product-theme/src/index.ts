/**
 * Product theme layer, node half.
 *
 * The layer is browser presentation (alias-token overrides over the active
 * theme); the node half has nothing to mount, so it stays the empty apply the
 * loader seat expects.
 */

/** Host plugin body — no host surface; the browser half owns the token layer. */
export function apply(): void {}
