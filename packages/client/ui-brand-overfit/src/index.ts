/**
 * Own-brand plugin (拟合 / Overfit), node half.
 *
 * The brand is browser presentation only — sidebar and hero occupants — so the
 * node half stays the empty apply the loader seat expects.
 */

/** Host plugin body — no host surface yet; the browser half owns the brand. */
export function apply(): void {}
