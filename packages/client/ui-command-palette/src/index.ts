/**
 * Command palette plugin, node half.
 *
 * The palette is browser presentation only — one `shell.overlay` occupant —
 * so the node half stays the empty apply the loader seat expects.
 */

/** Host plugin body — no host surface; the browser half owns the palette. */
export function apply(): void {}
