/** Host loader entry for the live-demo banner's browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior; the banner lives in the `./client` half. */
export function apply(): void {}

/** The banner element id, exported for tests and documentation. */
export const LIVE_DEMO_BANNER_ID = 'dsh-live-demo-banner'
